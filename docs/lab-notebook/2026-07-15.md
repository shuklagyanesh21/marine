# 2026-07-15

## SmORFinder full hybrid run — closed out

The hybrid SLURM run launched 2026-07-13 (`logs/smorfinder-hybrid-20260713-133555.log`)
finished 2026-07-14. No re-run planned.

### Phase markers (from the run log)

- `=== PHASE1_DONE: promoting failures ===`
- Promote: Total 112,414; Kept single 111,821; Promoted to meta 593
- `=== PROMOTED: starting phase 2 (meta) ===`
- `=== HYBRID_COMPLETE ===`

### Nextflow summaries

| Phase | Completed at | Duration | Succeeded | Ignored | CPU-hours |
|-------|--------------|----------|----------:|--------:|----------:|
| 1 (`single`) | 14-Jul-2026 22:29:21 | 1d 8h 40m 17s | 111,030 | 593 | 799.3 |
| 2 (`meta`) | 14-Jul-2026 22:42:17 | 12m 7s | 279 | 317 | 13.5 |

Phase-2 "Succeeded: 279" includes the 3 utility processes (PREPARE / PREFLIGHT / SUMMARIZE);
true meta genome successes = **276**.

### Final coverage (authoritative: `_SUCCESS.json` + run manifest)

| | Count |
|--|------:|
| Input genomes | 112,414 |
| `single` successes | 111,821 |
| `meta` successes | 276 |
| Hard failures (`pending` in manifest) | 317 |
| Coverage | 112,097 / 112,414 (**99.72%**) |
| Total predicted smORFs | 474,562 (mean ≈ 4.2; 12,240 zero-prediction successes) |

Hard failures are almost entirely GORG SAGs: **315 GORG + 2 MarDB**. Regenerable list:

`results/tables/smorfinder_hard_failures.tsv`

(from `data/processed/smorfinder_run_manifest.tsv`; see `docs/smorfinder-pipeline.md`).

### Corrections to earlier notes

- The early concurrent Prodigal probe (~19% single-mode crash) was **inflated by resource
  contention** under parallel load (many exit 134 / Abort). The full-run single-mode failure
  rate was **0.53%** (593 / 112,414). Meta recovery brought residual hard failures to **0.28%**.
- `grep -c "Error is ignored"` on the Nextflow log **overcounts**: the same NOTE lines are
  reprinted on every status refresh. Use the Nextflow run summary `Ignored` count or unique
  sample tags instead.
- `pending_inputs.tsv` after phase 2 is **stale** (still lists the 593 genomes queued at phase-2
  start). Prefer the run manifest / `_SUCCESS.json` counts.

### Docs updated

- `docs/smorfinder-pipeline.md` — full-run results table, corrected failure rate, meta resource
  defaults (2 CPU / 4 GB), hard-failure regeneration command
- `README.md` — marked run finished with coverage pointer
- `results/tables/smorfinder_hard_failures.tsv` written (gitignored under `results/`)
- Appended 3 SmORFinder slides to `docs/marine_genome_download_pipeline.pptx` (now 19 slides):
  section divider, how the hybrid run was executed, results & output paths. Same slides also
  wired into `scripts/generate_tier_pptx.py` so a full regenerate keeps them.
